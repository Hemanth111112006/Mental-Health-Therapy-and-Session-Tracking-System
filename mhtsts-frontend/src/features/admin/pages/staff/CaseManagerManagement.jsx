import React from 'react';
import StaffManager from '../../components/StaffManager';

const CaseManagerManagement = () => {
  return (
    <StaffManager 
      role="CASE_MANAGER" 
      title="Case Managers Directory" 
      description="Manage social workers and care coordinators. Oversee active external referral databases and community linkages." 
    />
  );
};

export default CaseManagerManagement;
